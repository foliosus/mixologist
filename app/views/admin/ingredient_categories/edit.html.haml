- page_title "Edit #{@ingredient_category.name}"

= simple_form_for(@ingredient_category, url: admin_ingredient_category_path(@ingredient_category)) do |f|
  = render 'form', f: f
  = f.submit 'Save your changes'
