- page_title 'Garnishes'

%table
  %thead
    %tr
      %th Name
      %th

  %tbody
    - @garnishes.each do |garnish|
      %tr
        %td= garnish.name
        %td= link_to Icon.new(:trash), garnish, data: {confirm: 'Are you sure?', method: :delete}, :'aria-hidden' => true

- content_for :secondary_content do
  %p= link_to "#{Icon.new(:add)} Add a garnish".html_safe, new_garnish_path, class: "call_to_action"
